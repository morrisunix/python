
def search(number):
    """ Returns if the nimber has been found on the array using a binary search

    Parameters
    ----------
    number: int
        The numebr you are searching

    Returns
    -------
    bool
        True if found, False if doesn't
    """

        
